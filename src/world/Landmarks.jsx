// Landmarks slot — currently empty; the four anchor landmarks have been
// folded into the city photo-cards. Reserved for bespoke 3D assets when
// real meshes are produced.
export default function Landmarks() {
  return null
}
