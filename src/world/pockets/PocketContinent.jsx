// PocketContinent is now a no-op — the world is photo-driven, not
// procedural. The HDRI sky + glass ocean in WorldStage carry the scene,
// and the floating photo cards (cities, landmarks, listings) provide
// the geography. Kept as a slot for future per-pocket bespoke scenes.
export default function PocketContinent() {
  return null
}
